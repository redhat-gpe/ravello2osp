  {{ name }}_flavor:
    type: OS::Nova::Flavor
    properties:
      ram: "{{ memory }}"
      vcpus: "{{ cpu }}"
      disk: "{{ disk }}"
      name: 
         list_join: ['-', [{ get_param: project_name }, "{{ name }}"]]
      is_public: False

